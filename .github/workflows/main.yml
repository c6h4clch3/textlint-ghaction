name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: ./
        id: textlint
        with:
          silent: "true"
      - run: "echo '${{ steps.textlint.outputs.result_logs }}'"
